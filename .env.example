# Local environment for hgi-analytics. Copy to .env (gitignored) and fill in.
# Ask Tom for the real values.
#
# Snowflake creds live elsewhere by design:
#   - dbt:  dbt/profiles.yml (shipped via .bothy.yml copy:)
#   - snow CLI: .snowflake/config.toml (shipped via .bothy.yml copy:)

# Lightdash (used by lightdash/migrations/*.py)
LIGHTDASH_URL=https://lightdash.hgi.tomeehan.net
LIGHTDASH_TOKEN=
LIGHTDASH_PROJECT_UUID=
LIGHTDASH_SPACE_UUID=
